#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-cp/bin/grunt-cp" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-cp/bin/grunt-cp" "$@"
  ret=$?
fi
exit $ret
